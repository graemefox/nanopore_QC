name: ont_qc_pipeline
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - samtools
  - minimap2
  - nanoplot
  - seqtk
  - nextflow=25.10.4  # pin to 25.x — epi2me-labs/wf-alignment uses Groovy imports incompatible with Nextflow 26+
  - singularity>=3.6.0
